name: CI

on:
  pull_request:
  push:
    branches-ignore:
      # Em main quem verifica é o workflow de deploy, que roda os mesmos
      # passos antes de publicar — rodar os dois seria trabalho duplicado.
      - main

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verificar:
    name: Tipos, build e regras de segurança
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      # O emulador do Firestore roda sobre a JVM.
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Instalar dependências
        run: npm ci

      - name: Instalar dependências das Cloud Functions
        run: npm ci --prefix functions

      - name: Checagem de tipos (cliente + servidor)
        run: npm run typecheck

      - name: Build do site
        run: npm run build:client

      - name: Testar as regras do Firestore
        run: npm run test:rules
